experiment:
  name: maau_baseline_p10
  seed: 0
  output_dir: experiments/

data:
  train_csv: data/metadata/isic2018_task1_train_labeled_p10.csv
  val_csv: data/metadata/isic2018_task1_val.csv
  test_csv: data/metadata/isic2018_task1_test.csv
  image_key: image_url
  mask_key: mask_url
  image_size: 256
  num_workers: 4
  augmentations: maau

model:
  type: maau
  in_channels: 3
  out_channels: 1
  encoder_filters: [64, 128, 256, 512]
  decoder_filters: [512, 256, 128, 64]
  use_attention_gates: true
  use_antialiasing_pool: true
  final_activation: sigmoid

training:
  batch_size: 8
  epochs: 50
  device: cuda
  mixed_precision: false
  early_stopping_patience: 15

loss:
  type: dice_bce
  dice_weight: 0.5
  bce_weight: 0.5

optimizer:
  type: adam
  lr: 1.0e-4
  weight_decay: 0.0

scheduler:
  type: cosine_annealing
  t_max: 100
  min_lr: 1.0e-6

metrics:
  - dice
  - iou
  - precision
  - recall
  - specificity

logging:
  save_every_epochs: 5
  best_model_metric: dice
  results_dir: reports/metrics/maau_baseline/
  save_debug_predictions: true
